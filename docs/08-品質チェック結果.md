# 08. 品質チェック結果

Phase 3 の完了後に行った、**全体的な品質チェック**の結果と、その修正の進み具合の記録です。

- 調べた日: 2026 年 9 月 22 日
- 調べたもの: バックエンド(`backend/`)、フロントエンド(`frontend/`)、リポジトリ全体(ドキュメント、設定、Docker、Git)
- 比べたもの: `docs/01`〜`07`、`README.md`、`CLAUDE.md`、Rails・Next.js・React・TypeScript の標準的なやり方

## 1. 調べ方と、限界

| 項目 | 内容 |
|---|---|
| 調べ方 | 3 つの観点(バックエンド / フロントエンド / リポジトリ全体)を、**読み取り専用**で調べた(ファイルは、何も変えていない)。コードを読み、検査ツールを実行した |
| 実行した検査 | rubocop、brakeman、bundler-audit、`bundle outdated`、`npm audit`、`npm outdated`、`tsc --noEmit`、`eslint` |
| 実行していないもの | テスト(RSpec、Vitest)、ビルド、マイグレーション、書き込みを伴う操作 |
| **★ 確認した** | 私(Claude)が、コードを読む、コマンドを実行する、実際のブラウザで試す、のいずれかで、**事実と確かめた**もの |
| **△ 推測** | 調査が、コードや Rails・ブラウザの仕様から、そう読める、としたもの。**実際に動かして確かめていない** |
| 注意 | 行番号は、調査時点のものです。**修正の前に、必ず、コードを読み直します** |

## 2. 全体の評価

**大きな問題は、見つかりませんでした。** 特に、次の点は、良い状態です。

- **検査ツールは、すべて緑**: rubocop 指摘 0 件、brakeman 警告 0 件、bundler-audit 脆弱性なし、`npm audit` 0 件、型検査・ESLint エラーなし
- **`docs/02`(API 仕様)と `docs/05`(ER 図)は、実装と、正確に一致**していた(ルート、ステータスコード、エラー文言、スキーマ、インデックス)
- **秘密情報は、コミットされていない**(`master.key`、`.env`、`.pem` は、履歴にもない)
- **入力の扱いが堅い**(許可する項目の明示、全角空白の除去、期限の不正値、enum の検証)。**XSS 対策**(React の自動エスケープ、`dangerouslySetInnerHTML` を使っていない)
- **アクセシビリティ**(キーボード操作、日本語の読み上げ、期限切れの文字表示、コントラスト比の自動テスト)
- 移動・並べ替えの規則を、React に依存しない**純関数に分けて**、細かくテストしている
- 開発の進め方(docs → issue → ブランチ → Pull Request)が、104 コミットのあいだ、一貫している

**ドキュメントと実装の不一致は、少なかった**です。不一致の多くは、ドキュメント同士(`docs/06` と `docs/07`)の、古い記述でした。

一方で、**実害のある不具合が 3 件**(H1〜H3)見つかりました。どれも、ふだんの 1 人での使い方では起きにくい(同時に 2 つの操作が重なったときに起きる)ため、これまでのテストと、手での確認では、見つかっていませんでした。

## 3. 指摘の一覧

**進み具合**の列: 「未」= 未対応、「Q2」など = その段階で対応する予定、「済」= 対応済み。

### 3.1 高(実害のある不具合、または、公開前に直す)

| # | 内容 | 確認 | 場所(修正前に読み直す) | 段階 | 進み |
|---|---|---|---|---|---|
| **H1** | **バックエンド: スキルの追加(create)と削除(destroy)が、ロックを取らない。** 追加は、ロックなしで「いまの最大の並び順 + 1」を決める。削除は、ロックの外で読んだ古い状態で、並び順を詰め直す。**同時に 2 件追加すると、同じ並び順ができる**。移動(`SkillMover`)は、全行ロックで守られているのに、追加・削除は、その外にある。`docs/05` の規則「並び順は 0 から連番」が、壊れうる。**同時実行のテストが 1 つもない**ので、見逃されていた | ★ コードで確認(実際の競合は、動かして再現していない) | `backend/app/controllers/api/v1/skills_controller.rb`(create、destroy)、`backend/app/models/skill.rb`(`next_position`、`renumber_positions`) | Q2 | 未 |
| **H2** | **フロントエンド: 移動が失敗したとき、通信中に完了した「追加・編集・削除」を、画面から巻き戻す。** `commitMove` は、ドロップした時点の一覧の全体を保存し、失敗時に、丸ごと戻す。通信中(最大 10 秒)も、追加・編集・削除は、できる。サーバーには保存済みなのに、画面から消える(削除したカードは、復活する)。再読み込みまで、画面とサーバーが、食い違う | ★ コードで確認 | `frontend/components/Board.tsx`(`commitMove`) | Q3 | 未 |
| **H3** | **フロントエンド: 移動が、時間切れ・接続断のとき、結果が分からないのに、「元に戻した」と断定する。** サーバーでは、成功しているかもしれない。削除は、404 のときに、一覧を取り直して、自己修復するが、移動の、それ以外の失敗には、手当てがない | ★ コードで確認 | `frontend/components/Board.tsx`、`frontend/lib/api.ts` | Q3 | 未 |
| **H4** | **CI(GitHub Actions)がない。** 検査は、手元で実行する約束だけで、機械的に、守らせる仕組みがない。`backend/bin/ci` と `npm run check` があるので、最小の CI を作れる | ★ 確認 | `.github/`(なし) | Q4 | 未 |
| **H5** | **デプロイ準備の穴**(第 4 部の P1 で扱う): ① `.env.production.example` が、`.gitignore` の `.env.*` に該当して、**無視される** ② 本番の DB ユーザー名が、`backend` 固定で、環境変数化されていない ③ フロントエンドの Dockerfile、Nginx の設定、本番用 Compose が、まだない | ★ ①②を確認 | `.gitignore`、`frontend/.gitignore`、`backend/config/database.yml` | Q4(①)、Q7(②③) | 未 |
| **H6** | **`docs/06` の「EC2 の 80 番を開ける」の書き方が、`docs/07`(自分の IP だけ)と、食い違う。** 認証がないアプリなので、誤読すると、世界に公開しかねない | ★ 確認 | `docs/06-技術スタック.md` の 2 章 | Q1 | 未 |

### 3.2 中

**バックエンド**

| # | 内容 | 確認 | 段階 | 進み |
|---|---|---|---|---|
| M1 | ロック待ち・デッドロックの例外(`Deadlocked`、`LockWaitTimeout`)を rescue せず、500 になる(ロック待ちは、最大 50 秒)。エラーの形も、API の形式と違う | 調査 | Q2 | 未 |
| M2 | **RSpec のランダム順が、コメントアウトされている**(`spec_helper.rb` の `=begin … =end` の中)。テストの順序への依存が、隠れる | ★ 確認 | Q4 | 未 |
| M3 | JSON 以外(フォーム形式)の POST も、受け付けうる。他のサイトからのフォーム送信で、スキルが作られうる(CORS のプリフライトがない)。`Content-Type` が JSON でなければ、415 にする | △ 推測 | Q5 | 未 |
| M4 | `note` に空文字を送ると、NULL ではなく `""` で保存される(`docs/02` は「空で消せる」)。`due_date` が、`YYYY-MM-DD` 以外や、範囲外の年のとき、意図しない値になる・500 になる可能性 | △ 推測 | Q5 | 未 |
| M5 | 本番設定: `force_ssl`/`assume_ssl` が、HTTP のみの構成と合うか未検証。`config.hosts` が未設定。Thruster と Nginx の二重プロキシ。本番イメージに、テスト・開発用の gem が入る(`BUNDLE_WITHOUT`) | 一部 △ | Q7 | 未 |
| M6 | DB に CHECK 制約がない(規則は、アプリ側だけで保証している)。`docs/05` の方針は「アプリ側で保証」なので、強化の提案 | 調査 | Q8 | 未 |
| M7 | `filter_parameter_logging` に `:note` がない(「ポイント・考察」の全文が、ログに残る) | 調査 | Q5 | 未 |
| M8 | 追加・削除のロジックが、コントローラーとモデルに散らばっている(移動・並べ替えは、サービス)。H1 の原因 | 調査 | Q2 | 未 |

**フロントエンド**

| # | 内容 | 確認 | 段階 | 進み |
|---|---|---|---|---|
| M9 | API の応答を検証していない(型の断言 `as ApiSkill` だけ)。形が変わると、「優先度: undefined」の表示や、カードが黙って消える。`fetch` の同じ処理が、5 か所に書かれている(共通の `request()` に集約できる) | 調査 | Q6 | 未 |
| M10 | 取得失敗の画面に、サーバー内部の API の URL が、ブラウザに表示される(本番で、内部のホスト名が見える) | 調査 | Q6 | 未 |
| M11 | セキュリティヘッダー(CSP、`X-Frame-Options` など)が未設定。Nginx で付けるか、`next.config.ts` で付けるか、決めていない | 調査 | Q7 | 未 |
| M12 | `<main role="alert">`(`loading.tsx`、`error.tsx`、`LoadError.tsx`)が、`main` のランドマークを上書きしている | 調査 | Q6 | 未 |
| M13 | フォームの検証エラーで、最初の不正な項目に、フォーカスが移らない。入力を直しても、エラーの文言が残る | 調査 | Q6 | 未 |
| M14 | `columns` を、描画のたびに、新しく作っている(`useMemo` がない)。`Column` の `idsKey` の回避策(クラッシュ対策)は、この根本原因の後始末 | 調査 | Q6 | 未 |
| M15 | `Board.tsx` が 337 行で、責務が大きい(状態 8 個、ref 5 個、API 5 種、ドラッグ、フォーカス、お知らせ)。H2 の競合を、まとめて扱いにくい | 調査 | Q8 | 未 |
| M16 | ダイアログが、ブラウザの機能で閉じられたとき、React の状態と食い違うかもしれない(Esc の連続押し)。`onClose` を購読していない | △ 推測(本物の Chrome で試したが、再現できなかった) | Q8 | 未 |
| M17 | 依存更新の仕組み(Dependabot)がない。`npm audit` が CI にない。Node のバージョン固定(`.nvmrc`、`engines`)がない | 調査 | Q4 | 未 |

**ドキュメント・リポジトリ**

| # | 内容 | 確認 | 段階 | 進み |
|---|---|---|---|---|
| M18 | `docs/01`・`docs/06`・`CLAUDE.md` の、コストの記述(「無料枠 30GB」「0.01 USD の予算アラート」など)が、`docs/07`(クレジット方式、未確認あり)より、断定的。`docs/06` の進捗表が古い。`docs/06` の「手順は README に書く」(実際は `docs/07`)。`docs/05` の記述と、実装のずれ。`docs/03` に、404 のお知らせなど、実装にあって文書にない挙動 | ★ 一部確認 | Q1 | 未 |
| M19 | マージ済みのブランチが、32 本残っている(GitHub の「マージ後に自動削除」が、オフ) | ★ 確認 | Q4 | 未 |
| M20 | `backend/README.md` が、Rails の雛形のまま | 調査 | Q7 | 未 |
| M21 | `backend/start.sh` と `SKILL.md` で、「他のプロジェクトのコンテナを止める・止めない」の記述が食い違う。Docker が動いていないときの扱いが、`frontend/start.sh` と違う | 調査 | Q7 | 未 |

### 3.3 低(まとめて、後で。段階 Q8)

- **バックエンド**: enum の整数値の扱いが、API 間で不揃い / `Skill.find("1abc")` が id 1 として扱われる / CORS の `max_age` / エラーメッセージの i18n 化 / 2 つのサービスのエラーの持ち方の不揃い / `wrap_parameters` の明示 / puma のバージョン上限 / 使っていない `en.yml`・`ApplicationJob` / `/up` が DB を確認しない
- **フロントエンド**: `SkillInput.priority` が `string` / 非 null 断言(`!`)が 2 か所 / `Modal.tsx` に、テスト用(jsdom)の分岐 / 本番で使われない `sortByPriority` / 使われていない CSS(`.drop-line`)/ `fixtures.ts` の置き場所 / `window.location.reload()` の使用 / `global-error`・`not-found` がない / 一人称のコメントが残っている / 「動きを減らす」設定で、ドラッグ終了時の動きが残る / 確認ダイアログが `alertdialog` でない / ダイアログの背景クリックの誤動作 / 日付を作る処理が、ロケールの出力形式に依存(`en-CA`)
- **リポジトリ**: LICENSE・説明文がない(非公開なので、低い)/ `.editorconfig` がない / `.gitignore` の細かい不足 / コミット規約が、文書にない / カバレッジ計測がない / README に、初回セットアップの 3 ステップがない / `prototype/` の位置づけの明記

## 4. 修正の進み具合(段階)

**1 段階ずつ**、issue → ブランチ → Pull Request → マージ、の順で進めます。

| 段階 | 内容 | 種類 | 対応する指摘 | 進み |
|---|---|---|---|---|
| **Q0** | この品質チェックの結果を、記録する(この文書) | docs のみ | 全部 | この Pull Request |
| **Q1** | ドキュメントの食い違いを直す | docs のみ | H6、M18 | 未 |
| **Q2** | **追加・削除を、ロックつきのサービスにする**+ 同時実行のテスト+ ロック待ち・デッドロックの応答 | コード | H1、M1、M8 | 未 |
| **Q3** | **移動失敗時の巻き戻しを、そのスキルだけにする**+ 結果不明の失敗は、一覧を再取得+ 移動の通信中は、追加・編集・削除も止める+ テスト | コード | H2、H3 | 未 |
| **Q4** | **開発の基盤**: GitHub Actions、RSpec のランダム順、`.gitignore`、`.nvmrc`/`engines`、Dependabot、ブランチの自動削除 | 設定 | H4、H5①、M2、M17、M19 | 未 |
| **Q5** | バックエンドの堅牢化: JSON 以外は 415、`note` の空文字、`due_date` の検証、ログの `:note`、`wrap_parameters` | コード | M3、M4、M7 | 未 |
| **Q6** | フロントエンドの堅牢化: 応答の検証と `request()`、内部 URL を出さない、`main` の role、フォームのフォーカス、`useMemo` | コード | M9、M10、M12、M13、M14 | 未 |
| **Q7** | **デプロイ準備(`docs/07` の P1 と統合)**: フロントエンドの Dockerfile、Nginx、本番用 Compose、本番の DB 環境変数、`hosts`・`force_ssl`、Thruster、`BUNDLE_WITHOUT`、`backend/README.md`、`start.sh` | コード | H5②③、M5、M11、M20、M21 | 未 |
| **Q8**(任意) | `Board.tsx` の分割、CHECK 制約、`Modal` の `onClose`、残りの「低」 | リファクタ | M6、M15、M16、低 | 未 |

- **推奨の順**: Q0 → Q1 → **Q2 → Q3**(実害のある不具合)→ Q4 → Q5 → Q6 → Q7 → Q8
- **Q2 と Q3 は、デプロイの前に**、直すのが望ましい(本番のデータは、壊れると、困る)
- 各段階で: わざとバグを入れて、テストが見つけるかを確認する。`bin/ci`(バックエンド)と `npm run check`(フロントエンド)を通す。フロントエンドは、実際の Chrome でも確認する。Pull Request に、**確認したことと、確認できていないこと**を、正直に書く
- AWS には、何も作らない(この品質チェックと修正は、コードとドキュメントだけ)

## 5. 確認状況の、まとめ

- **★ 私が確認した**: H1〜H6、M2、M19、M18 の一部(`docs/06` の記述など)。検査ツール(rubocop、brakeman、bundler-audit、`npm audit`、`tsc`、`eslint`)の結果
- **△ 推測(動かして確かめていない)**: M3、M4 の一部、デッドロックの実際の発生、M5 の `force_ssl` の実際の挙動、M16(本物の Chrome で試したが、再現できなかった)
- **調査が読んだ、細かい行番号**は、調査時点のもの。**修正の前に、必ず、読み直す**
