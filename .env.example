# 開発用の設定の見本。変えたいときは、このファイルを .env にコピーして使う
# (.env は Git に入れない。なくても、下の既定値で動く)

# MySQL の root パスワード。開発用の DB は、自分のパソコンの Docker の中だけにあり、外には公開されない
DB_PASSWORD=password

# API を呼んでよいオリジン(カンマ区切り)。開発中の Next.js
CORS_ORIGINS=http://localhost:3000
